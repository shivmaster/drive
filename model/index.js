const User = require('./userModel');
const moments = require('./moments.model');
const stories = require('./stories.model');
const memes = require('./memes.model')
const follow = require('./follow.model');
module.exports = {
    User,
    moments,
    stories,
    memes,
    follow
}